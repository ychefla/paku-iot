apiVersion: 1

providers:
  - name: 'EcoFlow'
    orgId: 1
    folder: 'EcoFlow'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards

