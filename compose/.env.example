# Paku IoT Stack Environment Variables
#
# This file documents all environment variables required by the unified Docker Compose stack.
# Copy this file to `.env` in the same directory and fill in your values:
#
#   cp .env.example .env
#
# Then edit `.env` with your actual credentials and settings.
# The `.env` file is git-ignored and should never be committed.

# ============================================================
# HETZNER CONFIGURATION
# ============================================================
# HETZNER host and credentials for the paku-iot-deploy tool
HETZNER_HOST=static.107.192.27.37.clients.your-server.de
HETZNER_USER=root
HETZNER_SSH_PUB_KEY_PATH=~/.ssh/ychefla-GitHub.pub
HETZNER_SSH_PRIV_KEY_PATH=~/.ssh/ychefla-GitHub
HETZNER_PAKU_DIR=/home/paku/paku-iot

# ============================================================
# DATABASE CONFIGURATION
# ============================================================
# PostgreSQL credentials for the paku_postgres container
POSTGRES_USER=paku
POSTGRES_PASSWORD=paku
POSTGRES_DB=paku

# ============================================================
# MQTT BROKER CONFIGURATION
# ============================================================
# Mosquitto MQTT broker settings
MQTT_HOST=mosquitto
MQTT_PORT=1883

# ============================================================
# COLLECTOR SERVICE CONFIGURATION
# ============================================================
# Settings for the MQTT → Postgres collector service
COLLECTOR_MQTT_TOPIC=paku/ruuvi/van_inside
COLLECTOR_DB_HOST=postgres
COLLECTOR_DB_PORT=5432

# ============================================================
# RUUVI EMULATOR CONFIGURATION
# ============================================================
# Settings for the sensor emulator
EMULATOR_MQTT_TOPIC=paku/ruuvi/van_inside
EMULATOR_PUBLISH_INTERVAL=5
# Maximum runtime in seconds (default: 300 = 5 minutes)
# Set to 0 for unlimited runtime (useful for local development)
MAX_RUNTIME_SECONDS=300

# ============================================================
# GRAFANA CONFIGURATION
# ============================================================
# Grafana admin credentials (change in production!)
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin

# ============================================================
# ECOFLOW CONFIGURATION (OPTIONAL)
# ============================================================
# To enable EcoFlow data collection, obtain API credentials from:
# https://developer.ecoflow.com/
# 
# Then set these values and start with:
# docker compose --profile ecoflow -f compose/stack.yaml up
#
ECOFLOW_ACCESS_KEY=your_access_key_here
ECOFLOW_SECRET_KEY=your_secret_key_here
ECOFLOW_DEVICE_SN=R331ZEB4ZEA1234567
# Set API endpoint based on your developer portal region:
# US: https://api.ecoflow.com (default)
# EU: https://api-e.ecoflow.com
ECOFLOW_API_URL=https://api-e.ecoflow.com

# ============================================================
# NOTES
# ============================================================
# - These are DEVELOPMENT defaults only
# - In production, use strong passwords and proper secrets management
# - Never commit .env files to version control
# - The .env file is git-ignored by default

# EcoFlow REST API Configuration
ECOFLOW_API_URL=https://api-e.ecoflow.com
REST_API_INTERVAL=30

# ============================================================
# OTA SERVICE CONFIGURATION
# ============================================================
# API key for admin endpoints (firmware upload, rollout management)
# Leave empty to disable authentication (not recommended for production)
OTA_API_KEY=your-secret-api-key-here
