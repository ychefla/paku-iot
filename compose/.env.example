# Paku IoT Stack Environment Variables
#
# This file documents all environment variables required by the unified Docker Compose stack.
# Copy this file to `.env` in the same directory and fill in your values:
#
#   cp .env.example .env
#
# Then edit `.env` with your actual credentials and settings.
# The `.env` file is git-ignored and should never be committed.

# ============================================================
# DATABASE CONFIGURATION
# ============================================================
# PostgreSQL credentials for the paku_postgres container
POSTGRES_USER=paku
POSTGRES_PASSWORD=paku
POSTGRES_DB=paku

# ============================================================
# MQTT BROKER CONFIGURATION
# ============================================================
# Mosquitto MQTT broker settings
MQTT_HOST=mosquitto
MQTT_PORT=1883

# ============================================================
# COLLECTOR SERVICE CONFIGURATION
# ============================================================
# Settings for the MQTT → Postgres collector service
COLLECTOR_MQTT_TOPIC=paku/ruuvi/van_inside
COLLECTOR_DB_HOST=postgres
COLLECTOR_DB_PORT=5432

# ============================================================
# RUUVI EMULATOR CONFIGURATION
# ============================================================
# Settings for the sensor emulator
EMULATOR_MQTT_TOPIC=paku/ruuvi/van_inside
EMULATOR_PUBLISH_INTERVAL=5

# ============================================================
# GRAFANA CONFIGURATION
# ============================================================
# Grafana admin credentials (change in production!)
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin

# ============================================================
# NOTES
# ============================================================
# - These are DEVELOPMENT defaults only
# - In production, use strong passwords and proper secrets management
# - Never commit .env files to version control
# - The .env file is git-ignored by default
