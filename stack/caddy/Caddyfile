{$PAKU_DOMAIN} {
    # OTA firmware service (strip /ota prefix before proxying)
    handle_path /ota/* {
        reverse_proxy ota-service:8080
    }

    # Grafana dashboard (catch-all)
    handle {
        reverse_proxy grafana:3000
    }
}
