# Paku IoT Stack Environment Variables
#
# This file documents all environment variables required by the unified Docker Compose stack.
# Copy this file to `.env` in the same directory and fill in your values:
#
#   cp .env.example .env
#
# Then edit `.env` with your actual credentials and settings.
# The `.env` file is git-ignored and should never be committed.

# ------------------------------------------------------------
# PostgreSQL Database Configuration
# ------------------------------------------------------------
# Username for the PostgreSQL database
POSTGRES_USER=paku

# Password for the PostgreSQL database
# IMPORTANT: Change this to a strong password in production
POSTGRES_PASSWORD=paku

# Name of the PostgreSQL database
POSTGRES_DB=paku

# ------------------------------------------------------------
# MQTT Broker Configuration
# ------------------------------------------------------------
# Hostname/IP of the MQTT broker (typically 'mosquitto' for Docker Compose)
MQTT_HOST=mosquitto

# Port for MQTT connections
MQTT_PORT=1883

# ------------------------------------------------------------
# Grafana Configuration (Optional)
# ------------------------------------------------------------
# Default admin password for Grafana
# IMPORTANT: Change this to a strong password in production
GF_SECURITY_ADMIN_PASSWORD=admin

# ------------------------------------------------------------
# Additional Configuration (for future use)
# ------------------------------------------------------------
# Add other environment variables here as the stack grows
# Examples:
# - API keys
# - External service URLs
# - Feature flags
